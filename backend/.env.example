# Database Configuration
# For local development (SQLite):
# DATABASE_URL=sqlite:///./inventory.db

# For production (PostgreSQL - Render will provide this):
# DATABASE_URL=postgresql://user:password@host:port/dbname

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend.vercel.app

# JWT Secret Key (generate a secure random string for production)
SECRET_KEY=your-secret-key-here-change-in-production

# Admin Default Password (optional - only used on first startup)
DEFAULT_ADMIN_PASSWORD=Admin!2025#Secure
